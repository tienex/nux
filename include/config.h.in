#ifndef CONFIG_H
#define CONFIG_H

#if @DEBUG@
#define DEBUG 1
#endif

#if @PLATFORM_VERBOSE@
#define PLATFORM_VERBOSE 1
#endif

// Backward compatibility
#ifdef PLATFORM_VERBOSE
#define PLT_VERBOSE PLATFORM_VERBOSE
#endif

#endif
