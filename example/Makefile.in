CPPFLAGS+= -I$(SRCROOT)/include -I$(SRCROOT)/@HALDIR@/include #-I$(SRCROOT)/libnux/include

LDFLAGS+=-L$(SRCROOT)/@HALDIR@ -L$(SRCROOT)/@PLTDIR@ -L$(SRCROOT)/libnux -T$(SRCROOT)/@HALDIR@/exe.ld
LDADD+=-lhal -lplt -lnux -u _nux_sysinit

PROGRAM=example

SRCS+= main.c
@LINK_LIBEC@
